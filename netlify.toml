# Configuración de build para Netlify
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Plugin oficial de Netlify para Next.js (maneja SSR, App Router, rutas API)
[[plugins]]
  package = "@netlify/plugin-nextjs"
